[Hook Scripts]
cros format = cros format --check --exclude "strings/locale/*" --commit ${PRESUBMIT_COMMIT} ${PRESUBMIT_FILES}
cros lint = cros lint --commit ${PRESUBMIT_COMMIT} ${PRESUBMIT_FILES}

[Hook Overrides]
branch_check: true
